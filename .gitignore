# Artéfacts de build (régénérés par build_app.sh)
dist/
.build/
.build-tmp/
.swiftpm/
Package.resolved

# Icône compilée (régénérée depuis Imprint.icon/ par app/make_icns.sh)
app/Imprint.icns

# Divers macOS
.DS_Store
